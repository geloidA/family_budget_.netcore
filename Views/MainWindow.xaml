<Window x:Class="family_budget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:family_budget"
        xmlns:vm="clr-namespace:family_budget.ViewModels"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="Default" TargetType="Label">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontFamily" Value="Inter"/>
        </Style>
    </Window.Resources>
    <Window.DataContext>
        <vm:MainWndViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="18"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Menu Background="Beige">
            <MenuItem Header="Войти" FontFamily="Inter"/>
        </Menu>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <lvc:PieChart DisableAnimations="True" />
                <lvc:PieChart DisableAnimations="True" Grid.Column="1" Series="{Binding Expense}"/>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <StackPanel>
                    <Label Content="Последние действия" Style="{StaticResource Default}" 
                   VerticalAlignment="Top"
                   HorizontalAlignment="Left"/>

                    <ListBox FontFamily="Inter"  x:Name="LastActions" ItemsSource="{Binding Expense}" />
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <Label Content="Личные траты:" Style="{StaticResource Default}" 
                   VerticalAlignment="Top"
                   HorizontalAlignment="Left"/>

                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
